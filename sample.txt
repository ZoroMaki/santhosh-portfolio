let found = [];
document.querySelectorAll('*').forEach(el => {
  if (el.scrollWidth > 375) {
    found.push({ tag: el.tagName, class: el.className, scrollWidth: el.scrollWidth, parent: el.parentElement?.className });
  }
});
console.table(found);